import React from "react";
import { StyledHeroTitle } from "./styles";

const HeroTitle: React.FC = () => {
  return (
    <StyledHeroTitle>
      Take the hustle, free up your mind, and start reading article
    </StyledHeroTitle>
  );
};

export default HeroTitle;
